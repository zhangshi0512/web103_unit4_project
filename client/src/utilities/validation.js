export const validateFeatures = (features) => {
  // Add validation logic here to ensure that features are valid based on specific requirements
  return features && Object.keys(features).length > 0;
};
